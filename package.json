{
  "name": "relay-incident-concierge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check app.js && node --check server.mjs && node --check scripts/record-demo.mjs",
    "demo:record": "node scripts/record-demo.mjs"
  },
  "dependencies": {
    "@novu/api": "^3.19.0"
  },
  "devDependencies": {
    "ffmpeg-static": "^5.3.0",
    "puppeteer-core": "^25.8.0"
  }
}
